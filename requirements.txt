pdfplumber>=0.11
camelot-py>=1.0
opencv-python-headless>=4.9   # camelot dependency
ghostscript>=0.7              # camelot lattice dependency — also needs the system `gs` binary
openpyxl>=3.1
pandas>=2.2
ollama>=0.4
pyyaml>=6.0
flask>=3.0
mcp[cli]>=2.0,<3

# Optional, for the OCR fallback (not wired up yet — see ingest.py TODOs)
# pytesseract>=0.3
# pdf2image>=1.17
